spring:
  application:
    name: review-mgt-service
  datasource:
    url: jdbc:mysql://localhost:3306/templeberg-prod?createDatabaseIfNotExist=true
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
